<shape
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:shape="rectangle"   >

    <solid
            android:color="@color/yellow" >
    </solid>

    <stroke
            android:width="1dp"
            android:color="@color/yellow" >
    </stroke>

    <corners android:radius="2dip"/>
    <corners android:bottomRightRadius="@dimen/five_dp"
        android:bottomLeftRadius="@dimen/five_dp"
        android:topLeftRadius="@dimen/five_dp"
        android:topRightRadius="@dimen/five_dp" />
    <padding android:left="@dimen/five_dp" android:top="@dimen/five_dp" android:right="@dimen/five_dp" android:bottom="@dimen/five_dp"/>

</shape>